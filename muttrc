# Paths ------------------------------------------------------------------------
set folder            = ~/.mail                    # mailbox location
set alias_file        = ~/.mutt/aliases            # where to store aliases
set header_cache      = ~/.mutt/cache/headers      # where to store headers
set message_cachedir  = ~/.mutt/cache/bodies       # where to store bodies
set certificate_file  = ~/.mutt/certificates       # where to store certificates
set mailcap_path      = ~/.mutt/mailcap            # entries for filetypes
set tmpdir            = ~/.mutt/temp               # where to keep temp files
set signature         = ~/.mutt/sig                # my signature file


# Basic Options ----------------------------------------------------------------
set mbox_type  = Maildir # mailbox type
set wait_key   = no      # shut up, mutt
set timeout    = 0       # idle time before scanning
set mail_check = 5       # minimum time between scans
set delete               # don't ask, just do
set quit                 # don't ask, just do!!
set beep_new             # bell on new mails
set pipe_decode          # strip headers and eval mimes when piping
set thorough_search      # strip headers and eval mimes before searching
unset move               # gmail does that
unset confirmappend      # don't ask, just do!
unset mark_old           # read/new is good enough for me


# Sidebar Patch --------------------------------------
set sidebar_delim   = '  │'
set sidebar_visible = yes
set sidebar_width   = 30
color sidebar_new color221 color233


# Status Bar -----------------------------------------
set status_chars  = " *%A"
set status_format = "───[ Folder: %f ]───[%r%m messages%?n? (%n new)?%?d? (%d to delete)?%?t? (%t tagged)? ]───%>─%?p?( %p postponed )?───"


# Header Options -------------------------------------
ignore *                                # ignore all headers
unignore from: to: cc: date: subject:   # show only these
unhdr_order *                           # some distros order things by default
hdr_order from: to: cc: date: subject:  # and in this order

# Alternate email addresses.
alternates raj.sahae@gmail.com

# Default inbox.
set spoolfile = "+rajsahae.gmail.com/INBOX"

# Other special folders.
set mbox      = "+rajsahae.gmail.com/Archive"
set postponed = "+rajsahae.gmail.com/Drafts"

# Mailboxes to show in the sidebar.
# mailboxes `echo ~/.mail/rajsahae.gmail.com/*`
mailboxes +rajsahae.gmail.com/INBOX \
          +rajsahae.gmail.com/Drafts \
          +rajsahae.gmail.com/Flagged \
          +rajsahae.gmail.com/Archive \
          +rajsahae.gmail.com/Dictionary \
          +rajsahae.gmail.com/Deals.3BTech \
          +rajsahae.gmail.com/Deals.Groupons \
          +rajsahae.gmail.com/Deals.LivingSocial \
          +rajsahae.gmail.com/Deals.Lot18 \
          +rajsahae.gmail.com/Forums.CodeProject \
          +rajsahae.gmail.com/Forums.CyberCoders \
          +rajsahae.gmail.com/Forums.Dice \
          +rajsahae.gmail.com/Forums.Southwest \
          +rajsahae.gmail.com/Forums.Vim \
          +rajsahae.gmail.com/Unroll.me \
          +temporary/search \


# Index View Options ---------------------------------
set date_format = "%m/%d"
set index_format = "[%Z]  %D  %-20.20F  %s"
set sort = threads                         # like gmail
set sort_aux = reverse-last-date-received  # like gmail
set uncollapse_jump                        # don't collapse on an unread message
set sort_re                                # thread based on regex
set reply_regexp = "^(([Rr][Ee]?(\[[0-9]+\])?: *)?(\[[^]]+\] *)?)*"


# Key Bindings ---------------------------------
bind index R        group-reply
bind index <space>  collapse-thread
bind index <tab>    sync-mailbox

# Gmail-style keyboard shortcuts
macro index ga "<change-folder>=rajsahae.gmail.com/Archive<enter>" "Go to all mail"
macro index gd "<change-folder>=rajsahae.gmail.com/Drafts<enter>" "Go to drafts"
macro index gi "<change-folder>=rajsahae.gmail.com/INBOX<enter>" "Go to inbox"
macro index gf "<change-folder>=rajsahae.gmail.com/Flagged<enter>" "Go to starred messages"
macro index gs "<change-folder>=rajsahae.gmail.com/Sent<enter>" "Go to sent mail"

# Shortcut for search folder
macro index g/ "<change-folder>=temporary/search<enter>" "Go to search folder"

# Vim style keyboard shortcuts
bind index gg       first-entry
bind index G        last-entry

# Ctrl-R to mark all as read
macro index \Cr "T~U<enter><tag-prefix><clear-flag>N<untag-pattern>.<enter>" "mark all messages as read"
# macro index \Cr "<limit>~N<enter><clear-flag>N.<enter><limit>all<enter>" "mark all messages as read"

# Saner copy/move dialogs
macro index C "<copy-message>?<toggle-mailboxes>" "copy a message to a mailbox"
macro index M "<save-message>?<toggle-mailboxes>" "move a message to a mailbox"

# Macro 'a' to limit all for easy un-limiting
macro index a "<limit>all<enter>" "show all messages (undo limit)"

# Urlview macro
macro pager \Cu "|urlview<enter>" "call urlview to open links"

# notmuch macro
macro index S "<enter-command>unset wait_key<enter><shell-escape>mutt-notmuch-py ~/.mail/temporary/search<enter><change-folder-readonly>+temporary/search<enter>" "search mail (using notmuch)"


# Sidebar Navigation ---------------------------------
bind index,pager <down>   sidebar-next
bind index,pager <up>     sidebar-prev
bind index,pager <right>  sidebar-open


# Pager View Options ---------------------------------
set pager_index_lines = 10 # number of index lines to show
set pager_context = 3      # number of context lines to show
set pager_stop             # don't go to next message automatically
set menu_scroll            # scroll in menus
set tilde                  # show tildes like in vim
unset markers              # no ugly plus signs
bind index,pager N search-opposite # Shift-N for reverse search, like Vim
set quote_regexp = "^( {0,4}[>|:#%]| {0,4}[a-z0-9]+[>|]+)+"


# Pager Key Bindings ---------------------------------
bind pager k  previous-line
bind pager j  next-line
bind pager gg top
bind pager G  bottom
bind pager R  group-reply


# Postpone/Recall Bindings -----------------------------
bind compose p postpone-message
bind index p recall-message

# View attachments properly.
bind attach <return> view-mailcap


# Compose View Options -------------------------------
set editor = "vim"                   # Use terminal Vim to compose email.
set realname = "Raj Sahae"           # who am i?
set envelope_from                    # which from?
set sig_dashes                       # dashes before sig
set edit_headers                     # show headers when composing
set fast_reply                       # skip to compose when replying
set askcc                            # ask for CC:
set fcc_attach                       # save attachments with the body
unset mime_forward                   # forward attachments as part of body
set forward_format = "Fwd: %s"       # format of subject when forwarding
set forward_decode                   # decode when forwarding
set attribution = "On %d, %n wrote:" # format of quoting header
set reply_to                         # reply to Reply to: field
set reverse_name                     # reply as whomever it was to
set include                          # include message in replies
set forward_quote                    # include message in forwards

# Goobook Integration -----------------------------------------------------
set query_command = "goobook query '%s'"
macro index,pager a "<pipe-message>goobook add<return>" "add sender to google contacts"
bind editor <tab> complete-query


# SMTP Settings
set sendmail      = "/usr/local/bin/msmtp"
set envelope_from = yes
set sendmail_wait = 0
unset record

macro generic "<esc>1" ":set from=rajsahae@gmail.com"
macro generic "<esc>2" ":set from=rsahae@teslamotors.com"


# Viewing HTML Email --------------------------------------
auto_view text/html                                  # view html automatically
alternative_order text/plain text/enriched text/html # save html for last
